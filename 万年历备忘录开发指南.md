# 万年历备忘录（个人桌面工具）开发指南 v14
> 适用源码：`MemoCalendarReminder.py`  
> 建议发布名称：`MemoCalendarReminder`（源码 + EXE 同名，便于长期维护）

---

## 1) 这个工具解决什么问题
这是一个 **本地桌面万年历 + 备忘录 + 到点提醒** 工具，面向“仅自己使用”的场景：
- 不依赖服务器、不依赖浏览器，离线可用
- 支持按日期快速筛选、搜索、排序、逾期处理
- 到点弹窗提醒（适合日常工作/项目跟进）

---

## 2) 功能清单（你当前版本具备）
### 2.1 日历与快速操作
- **月历视图**：左侧显示当月日历
- **单击日期 = 直接筛选**：点击某天，右侧列表只显示该日期的事项
- **今日按钮**：跳转到今天，并取消日期筛选（显示全部）
- **日历提示**（信息密度）  
  - 当天 1~3 条事项：日期格子底部显示 1~3 个小圆点  
  - 当天 ≥4 条事项：显示数字徽标（≥10 显示 9+）
- **双击日期 = 快速新增**：弹出快速新增窗口（支持 Ctrl+Enter 保存）

### 2.2 备忘录管理（右侧）
- 添加 / 编辑 / 删除
- 批量操作（支持多选）：**批量标记完成 / 批量恢复提醒 / 批量删除**
- 优先级：四象限（重要紧急等）
- 状态：待提醒 / 已提醒 / 已完成 / 逾期
- 搜索：关键词过滤
- 排序：时间↑↓、优先级高→低、状态、逾期优先

### 2.3 逾期处理与提醒增强
- 逾期高亮显示（未完成且时间已过去）
- 逾期一键处理：批量完成 / 批量恢复提醒
- **恢复并顺延 5 分钟提醒**：避免“恢复后立刻连弹”影响体验  
  （会设置顺延时间，到点再弹窗提醒）

### 2.4 迷你窗口（悬浮待办）
- 显示当前筛选结果/常用视图
- 事项多时支持滚动条与滚轮滚动

### 2.5 备份与恢复（右上角）
- **导出备份**：导出 JSON 文件（建议作为“迁移/换电脑/重装”备份）
- **导入备份**：支持覆盖或合并（ID 自动去重）

---

## 3) 目录与命名规范（建议长期用这一套）
建议你在 G 盘统一管理：

```
G:\python\
  memo_calendar\
    MemoCalendarReminder.py                 ← 主源码（建议改名）
    dist\                                   ← 打包后输出目录（PyInstaller）
    backups\                                ← 备份文件（导出 JSON 建议放这里）
```

### 推荐命名
- 源码：`MemoCalendarReminder.py`
- 打包后的工具：`MemoCalendarReminder.exe`
- 备份文件：`memo_backup_YYYYMMDD_HHMM.json`

> 说明：脚本名改不改都能运行，但统一命名有利于你后续升级、打包、找文件。

---

## 4) 运行方式（你常用：G 盘目录运行）
把源码放到 `G:\python\memo_calendar\` 后运行：

```bat
cd /d G:\python\memo_calendar
python MemoCalendarReminder.py
```

如果你的 Python 不在 PATH（例如便携版），用完整路径：
```bat
cd /d G:\python\memo_calendar
G:\python\python.exe MemoCalendarReminder.py
```

---

## 5) Python 环境检查（防止“装了但不会启用/测试”）
在 CMD 执行：

### 5.1 检查 Python 版本与路径
```bat
python --version
where python
```

### 5.2 检查 Tkinter（本工具必须）
```bat
python -c "import tkinter; print('tkinter ok')"
```

如果报 `_tkinter` 相关错误：说明 Python 缺少 Tcl/Tk（常见于精简版安装），建议安装官方完整版 Python。

### 5.3（可选）语法自检（不启动界面，只检查语法）
```bat
python -m py_compile MemoCalendarReminder.py
```

---

## 6) 数据保存与迁移（换电脑也不丢）
### 6.1 推荐的迁移方式（最稳）
- 右上角 **导出备份** → 得到 `memo_backup_*.json`
- 新电脑运行程序 → **导入备份**（覆盖或合并）

这是最稳的“跨电脑迁移”方式，不依赖你记住数据文件在哪里。

### 6.2 进阶：本地数据文件位置
程序一般会把数据保存在本地固定位置（例如同目录/用户目录/AppData）。  
如果你未来想统一管理，建议把“数据文件路径”固定到：
- `G:\python\memo_calendar\data\memo_data.json`

（如你需要，我可以帮你把数据目录改成固定路径，并自动创建目录。）

---

## 7) 打包成免 Python 环境的 EXE（Windows）
> 目标：即使别的电脑没装 Python，也能直接双击运行。

### 7.1 安装 PyInstaller（只需在打包电脑安装一次）
```bat
python -m pip install -U pyinstaller
```

### 7.2 一句打包命令（单文件、无控制台）
在源码所在目录执行：

```bat
pyinstaller --onefile --noconsole --name MemoCalendarReminder MemoCalendarReminder.py
```

如果你还没改名，用原文件名也可以：
```bat
pyinstaller --onefile --noconsole --name MemoCalendarReminder 万年历备忘录_增强版_UI再优化_v14_左400_日历正方格_修复y0.py
```

打包输出：
- `dist\MemoCalendarReminder.exe`

### 7.3 打包调试建议
第一次打包建议先不加 `--noconsole`（便于看报错）：
```bat
pyinstaller --onefile --name MemoCalendarReminder MemoCalendarReminder.py
```
确认没问题后再用 `--noconsole` 生成最终版本。

---

## 8) 常见警告：libpng iCCP（是否需要处理？）
你可能看到：
`libpng warning: iCCP: known incorrect sRGB profile`

这通常来自某些 PNG 资源的 ICC 配置，**不影响功能**。  
如果你不想看到控制台警告：
- 用 `--noconsole` 打包后用户也看不到
- 或后续再做“过滤输出”（一般没必要）

---

## 9) 日常使用建议（更像“工具”而不是“记事本”）
- 日历点击某天 → 只处理那天事项（效率最高）
- 用“逾期优先”排序 → 快速清空积压
- 备份：每周导出一次到 `backups\`（或网盘）
- 重要任务：用“恢复并顺延 5 分钟提醒”把注意力拉回

---

## 10) 下一步可扩展方向（建议路线）
### 10.1 提醒体验
- Snooze（5/10/30/60 分钟 + 自定义）
- 勿扰时段（夜间不弹窗，只记录）
- 错过补弹（电脑睡眠/重启后补提醒）

### 10.2 数据与性能（长期）
- JSON → SQLite（更稳、更快、支持索引/统计）
- 重复事件：规则驱动（不再批量生成大量记录）

### 10.3 交互效率
- 快捷键：Ctrl+F 搜索、Ctrl+N 新建、Del 删除、Ctrl+Z 撤销
- 数字徽标按状态颜色区分（待提醒红/完成绿）

---

## 11) 你应当保留的“发布包”（建议）
- `MemoCalendarReminder.py`（源码）
- `dist\MemoCalendarReminder.exe`（免环境工具）
- `backups\memo_backup_*.json`（备份文件，长期保存）

---

如你希望我把这份指南同时输出成 **PDF** 或放进程序的“帮助”菜单里，也可以继续做。
